# Litmanger — Academic Paper Manager
#
# Core dependencies (stdlib-only where possible)
requests>=2.28
browser-cookie3>=0.19

# Optional: richer BibTeX export
# bibtexparser>=1.4

# Optional: improved HTML parsing (fallback to stdlib html.parser)
# beautifulsoup4>=4.12
